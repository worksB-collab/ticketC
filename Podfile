# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ticketC' do
	pod 'SwiftyJSON', '~> 4.0'
	pod 'Alamofire', '~> 5.2'

  # Pods for ticketC

  target 'ticketCTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ticketCUITests' do
    # Pods for testing
  end

end
